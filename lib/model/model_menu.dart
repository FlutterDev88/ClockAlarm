import 'package:clock_app/const/enum.dart';
import 'package:flutter/widgets.dart';

class ModelMenu {
  MenuType type;
  String? title;
  IconData? icon;

  ModelMenu(this.type, this.title, this.icon);
}
